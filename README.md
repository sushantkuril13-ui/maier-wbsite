# React + Express Minimal Main Page

Files added:

- `server.js` — Express server serving `public/`.
- `public/index.html` — Main HTML loading React from CDN.
- `public/main.js` — Minimal React app (no build tool required).

Run locally:

```bash
npm install
npm start
```

Then open http://localhost:3000
